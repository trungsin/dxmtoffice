# AI Module

This module handles integration with AI providers (OpenAI, Claude) to support system-wide assistant features.

## Structure
- `service/`: Core logic and middleware for AI communication.
- `api/`: API endpoint definitions.

## Features
- Selectable provider (OpenAI/Claude).
- Request/Response logging in Dev mode.
- Middleware for consistent error handling.
